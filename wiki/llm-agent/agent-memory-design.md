# Agent Memory 模块设计（三层记忆）

> tags: llm-agent, memory, 短期记忆, 长期记忆, redis, 向量化
> weight: 6
> updated: 2026-08-04

## 核心结论
记忆分三层：短期=最近对话原文（Redis，会话级 TTL）；中期=对话摘要向量化（向量库，按时间衰减加权检索）；长期=用户画像宽表（结构化偏好/事实）。写入异步落盘，定时任务合并摘要、淘汰过期内容。

## 展开
- **短期记忆**：最近 N 轮对话原文，放 Redis（key: session_id），读写在请求主链路，随上下文窗口直接拼进 prompt。会话结束或 TTL 过期即清。
- **中期记忆**：会话/片段摘要（LLM 生成），向量化后存向量库。检索时按 query 相似度召回，打分带**时间衰减因子**（越久权重越低）。解决"上周聊过的事"这类跨会话引用。
- **长期记忆**：稳定的用户画像——偏好、身份事实、历史行为统计，结构化宽表（MySQL/HBase）。由离线任务从中期记忆中抽取沉淀。
- **写入路径**：异步（消息队列/后台任务），不阻塞对话响应。定时任务做：多条摘要合并去重、过期淘汰、冲突消解（新事实覆盖旧事实）。
- **读取路径**：请求时并行取三层——短期直接拼、中期向量检索 top-k、长期画像取相关字段，组装进 system prompt 或专门的 memory 段。
- **表结构**：三层大体一致：user_id、session_id、type（原文/摘要/画像）、content、embedding、created_at/expire_at。

## 关键细节 / 易错点
- 追问"对话轮数变多上下文超限，怎么做记忆压缩 / 有哪些压缩方法尽量不丢信息"：① 滑动窗口只留最近 N 轮原文 + 更早轮次滚动摘要（可层级摘要：摘要的摘要）；② 摘要时保留结构化事实清单（实体/结论/TODO），原文落库可回溯按需再取——"索引留指针、细节可回捞"是少丢信息的关键；③ tool 大返回裁剪只留相关字段；④ token 级压缩（LLMLingua 类）删冗余词。
- 追问"怎么控制单次会话 token 预算"：设预算上限并监控每轮 token 增长；超限先砍 tool 冗余输出，再把早期对话压缩成摘要，记忆检索 top-k 限量；极端情况拒绝继续并提示开新会话。
- 追问"记忆库怎么更新用户画像 / 短期长期怎么区分"：每轮会话异步抽取事实/偏好（LLM 抽取成结构化条目）写画像宽表，带置信度与时间戳，冲突时新覆盖旧或 LLM 仲裁；区分标准=是否跨会话稳定——会话内有效放短期（Redis 原文），跨会话稳定偏好/事实沉淀为长期（宽表 + 摘要向量库）。
- 记忆冲突：用户说"我不吃辣"后来又说"最近爱吃辣"——需要时间戳优先或让 LLM 做冲突消解。
- 记忆污染：错误信息一旦写入会持续影响后续对话，写入前要过质量门槛（置信度/来源校验）。
- 隐私合规：用户数据要可删除（右被遗忘），画像字段脱敏。
- 追问"为什么摘要而不是存原文"：原文太长、检索噪声大；摘要压缩信息密度，但要接受有损。
- 追问"时间衰减怎么实现"：score = 相似度 × e^(-λ·Δt)，λ 控制衰减速度；或分桶（近 7 天/30 天/更久）加权。

## 关联
- 相关知识点：[[wiki/database/database-selection-for-business.md]]、[[wiki/rag/rag-pipeline.md]]
- 常见追问链：三层怎么分 → 写入为什么异步 → 冲突/污染怎么处理 → 和 RAG 的区别

## 面经来源
- 快手 Agent 研发一面（2026-07）
- 美团 AI Agent 开发一面（2026-08）：长期画像和短期聊天历史分别存哪、怎么喂给大模型
- 美团 AI Agent 开发二面（2026-08）：对话轮数变多上下文超限，怎么做记忆压缩
- 阿里云 AI 应用研发一面（2026-08）：上下文越拉越长，哪些压缩方法能不丢信息控长度
- 快手 AI 应用开发一面（2026-08）：向量记忆库如何更新用户画像、短期/长期记忆如何区分
- 未知公司面经片段B（2026-08）：如何设计 Agent 的长期记忆
